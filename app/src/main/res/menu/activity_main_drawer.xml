<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menu_profile"
            android:icon="@drawable/ic_profile"
            android:title="@string/menu.profile" />
        <item
            android:id="@+id/menu_friends"
            android:icon="@drawable/ic_friends"
            android:title="@string/menu.frineds" />
        <item
            android:id="@+id/menu_conversations"
            android:icon="@drawable/ic_message"
            android:title="@string/menu.conversations" />
    </group>

    <item android:title="@string/menu.section.account">
        <menu>
            <item
                android:id="@+id/menu_account_logout"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/menu.account.logout" />
        </menu>
    </item>

</menu>
